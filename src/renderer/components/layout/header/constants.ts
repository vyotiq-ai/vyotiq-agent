/**
 * Header Constants
 *
 * Shared constants for header layout and styling.
 */

/**
 * Width reserved for the native window controls (minimize/maximize/close).
 * On Windows with titleBarOverlay, this is ~138px.
 */
export const WINDOW_CONTROLS_WIDTH = 138;

/** Header height in pixels — must match the titleBarOverlay height. */
export const HEADER_HEIGHT = 32;
